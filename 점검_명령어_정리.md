# 점검 명령어 정리

## 📋 개요

로컬 서버와 동국대 서버 점검을 위한 Ansible Playbook 실행 명령어를 정리합니다.

---

## 🖥️ 로컬 서버 점검 (Hana-PC)

### OS 점검
```bash
ansible-playbook -i inventory/dongguk_servers.ini redhat_check/redhat_check.yml
```

### WAS 점검
```bash
ansible-playbook -i inventory/dongguk_servers.ini tomcat_check/tomcat_check.yml
```

### MariaDB 점검
```bash
ansible-playbook -i inventory/dongguk_servers.ini mariadb_check/mariadb_check.yml
```

### PostgreSQL 점검
```bash
ansible-playbook -i inventory/dongguk_servers.ini postgresql_check/postgresql_check.yml
```

### CUBRID 점검
```bash
ansible-playbook -i inventory/dongguk_servers.ini cubrid_check/cubrid_check.yml
```

---

## 🌐 동국대 서버 점검 (192.168.0.23)

### OS 점검
```bash
ansible-playbook -i inventory/dongguk_remote_servers.ini redhat_check/redhat_check.yml
```

### WAS 점검
```bash
ansible-playbook -i inventory/dongguk_remote_servers.ini tomcat_check/tomcat_check.yml
```

### MariaDB 점검
```bash
ansible-playbook -i inventory/dongguk_remote_servers.ini mariadb_check/mariadb_check.yml
```

### PostgreSQL 점검
```bash
ansible-playbook -i inventory/dongguk_remote_servers.ini postgresql_check/postgresql_check.yml
```

### CUBRID 점검
```bash
ansible-playbook -i inventory/dongguk_remote_servers.ini cubrid_check/cubrid_check.yml
```

---

## 🚀 전체 점검 실행 (한 번에 모든 점검)

### 로컬 서버 전체 점검
```bash
# OS 점검
ansible-playbook -i inventory/dongguk_servers.ini redhat_check/redhat_check.yml

# WAS 점검
ansible-playbook -i inventory/dongguk_servers.ini tomcat_check/tomcat_check.yml

# MariaDB 점검
ansible-playbook -i inventory/dongguk_servers.ini mariadb_check/mariadb_check.yml

# PostgreSQL 점검
ansible-playbook -i inventory/dongguk_servers.ini postgresql_check/postgresql_check.yml

# CUBRID 점검
ansible-playbook -i inventory/dongguk_servers.ini cubrid_check/cubrid_check.yml
```

### 동국대 서버 전체 점검
```bash
# OS 점검
ansible-playbook -i inventory/dongguk_remote_servers.ini redhat_check/redhat_check.yml

# WAS 점검
ansible-playbook -i inventory/dongguk_remote_servers.ini tomcat_check/tomcat_check.yml

# MariaDB 점검
ansible-playbook -i inventory/dongguk_remote_servers.ini mariadb_check/mariadb_check.yml

# PostgreSQL 점검
ansible-playbook -i inventory/dongguk_remote_servers.ini postgresql_check/postgresql_check.yml

# CUBRID 점검
ansible-playbook -i inventory/dongguk_remote_servers.ini cubrid_check/cubrid_check.yml
```

---

## 📝 사용 방법

### WSL에서 실행
```bash
# 프로젝트 디렉토리로 이동
cd /mnt/c/ansible_server_check

# 점검 실행 (예: 로컬 서버 OS 점검)
ansible-playbook -i inventory/dongguk_servers.ini redhat_check/redhat_check.yml
```

### Windows PowerShell에서 실행
```bash
# WSL을 통해 실행
wsl bash -c "cd /mnt/c/ansible_server_check && ansible-playbook -i inventory/dongguk_servers.ini redhat_check/redhat_check.yml"
```

---

## 🔍 연결 테스트

### 로컬 서버 연결 테스트
```bash
ansible -i inventory/dongguk_servers.ini dongguk_servers -m ping
```

### 동국대 서버 연결 테스트
```bash
ansible -i inventory/dongguk_remote_servers.ini dongguk_servers -m ping
```

---

## 📊 점검 결과 확인

점검 결과는 자동으로 **192.168.0.18** API 서버로 전송되며, 다음 대시보드에서 확인할 수 있습니다:

- **통합 대시보드**: http://192.168.0.18:8000/api/report
- **OS 점검 리포트**: http://192.168.0.18:8000/api/os-checks/report
- **WAS 점검 리포트**: http://192.168.0.18:8000/api/was-checks/report
- **DB 점검 리포트**: http://192.168.0.18:8000/api/db-checks/report

---

## ⚙️ 서버 정보

### 로컬 서버 (Hana-PC)
- **Inventory 파일**: `inventory/dongguk_servers.ini`
- **호스트명**: `Hana-PC`
- **연결 방식**: `ansible_connection=local`

### 동국대 서버
- **Inventory 파일**: `inventory/dongguk_remote_servers.ini`
- **IP 주소**: `192.168.0.23`
- **SSH 포트**: `2222`
- **사용자**: `root`
- **연결 방식**: SSH (원격)

---

## 💡 팁

1. **점검 전 연결 테스트**: 점검 실행 전에 `ansible -m ping` 명령어로 연결을 확인하세요.
2. **상세 로그 확인**: 문제 발생 시 `-v` 옵션으로 상세 로그를 확인할 수 있습니다:
   ```bash
   ansible-playbook -i inventory/dongguk_remote_servers.ini redhat_check/redhat_check.yml -v
   ```
3. **특정 호스트만 점검**: `--limit` 옵션으로 특정 호스트만 점검할 수 있습니다:
   ```bash
   ansible-playbook -i inventory/dongguk_remote_servers.ini redhat_check/redhat_check.yml --limit dongguk_server1
   ```

---

**마지막 업데이트**: 2026년 1월 8일

